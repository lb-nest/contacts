services:
  db:
    image: postgres
    environment:
      - POSTGRES_DB
      - POSTGRES_PASSWORD
  server:
    build: .
    ports:
      - 3040:8080
    environment:
      - DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@db/${POSTGRES_DB}?schema=public
      - AUTHORIZATION_URL
    depends_on:
      - db
